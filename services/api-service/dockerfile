# Build Stage: Install dependencies, build the app, and prune dev dependencies
FROM node:22-alpine AS builder
WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

RUN npm prune --production

# Final Stage: Only copy what’s needed to run the app
FROM node:22-alpine
WORKDIR /app

# Copy production package files (if needed for runtime) and the built assets
COPY --from=builder /app/package*.json ./
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules

EXPOSE 3030

# Metadata labels
LABEL org.opencontainers.image.source="https://github.com/CS4366/SIREN"
LABEL org.opencontainers.image.title="SIREN API Service"

CMD ["node", "dist/api.js"]
